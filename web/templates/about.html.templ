<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>OpenFero - {{ .Title }}</title>
    <!-- Add theme-toggle script early in head to prevent flash -->
    <script src="/assets/js/theme-toggle.js"></script>
    <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="/assets/bootstrap-icons-1.11.3/font/bootstrap-icons.min.css">
    <script src="/assets/js/htmx.min.js"></script>
</head>

<body style="padding-top: 70px;">
    {{ template "navbar" . }}

    <!-- Page content-->
    <section class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0"><i class="bi bi-info-circle me-2"></i>About OpenFero</h4>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <tbody>
                                <tr>
                                    <th scope="row">Version</th>
                                    <td>{{ .Version }}</td>
                                </tr>
                                <tr>
                                    <th scope="row">Commit</th>
                                    <td>{{ .Commit }}</td>
                                </tr>
                                <tr>
                                    <th scope="row">Build Date</th>
                                    <td>{{ .BuildDate }}</td>
                                </tr>
                            </tbody>
                        </table>
                        <div class="mt-4">
                            <h5>About OpenFero</h5>
                            <p>OpenFero is an event-triggered job scheduler for code-agnostic recovery jobs. It enables automatic remediation of infrastructure and application issues based on alerts.</p>
                            <p class="mt-3">
                                <a href="https://github.com/OpenFero/openfero" target="_blank" class="btn btn-outline-secondary">
                                    <i class="bi bi-github me-2"></i>GitHub Repository
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Bootstrap and custom scripts -->
    <script src="/assets/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/js/timestamp-formatter.js"></script>
</body>

</html>
